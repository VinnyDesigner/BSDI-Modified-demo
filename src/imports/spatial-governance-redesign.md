Redesign the Spatial Governance page layout with the following changes while keeping the existing red theme, spacing system, and UI style consistent with other pages.

Layout Changes

Remove the “Access Roles” card on the left side of the map.

Remove:

Role one

Role two

Role three

Role four

Role five

“5 Active Roles”

“2,847 Users”

“LIVE” indicator

New Layout Structure

Split the top section into two panels:

Left Panel (40% width):

Add a table named “Access Roles”

This table replaces the old left sidebar.

The table should follow the same visual style as the existing Spatial Boundaries Registry table.

Right Panel (60% width):

Keep the map view

Resize map to fit the 60% width layout.

Maintain existing map controls and Spatial/Data toggle.

Access Roles Table

Table title: Access Roles

Columns:

Role Name

Department / Organization

Assigned Users

Status

Last Updated

Actions

Actions column:

Edit button

Edit Interaction Behavior

When the Edit button is clicked:

The selected role row enters edit mode.

The map highlights the spatial boundary linked to that role.

The map boundary becomes editable (draw / modify / reshape).

User can:

Edit the spatial boundary

Adjust the drawn area on the map

Show an “Update” button on the map panel.

Update Behavior

When Update is clicked:

The spatial boundary changes are saved.

The role in the Access Roles table is updated.

Exit edit mode.

Map returns to normal view.

Design Guidelines

Maintain clean enterprise dashboard style

Keep consistent red accent color

Use rounded cards, soft shadows, and proper spacing

Ensure table and map alignment are balanced

Make the interface minimal and easy to scan