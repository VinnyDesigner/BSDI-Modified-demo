Update the UI design of the following pages to use the same layout, structure, and visual style as the existing “Organizations Directory” component in the Organization page to maintain design consistency across the dashboard.

Replicate the Organizations Directory design pattern including:

Section header style

Search bar

Filter / action buttons

Table / list layout

Row structure

Icon style

Pagination style

Spacing and alignment

Card container style

Red theme color system used across the dashboard

Apply this same component structure to the following pages:

1. Spatial Governance Page

Replace the existing list section with a directory-style layout titled:

“Spatial Boundaries Registry”

Content structure should include:

Boundary Name

Boundary Type

Created By

Last Updated

Status

Actions (View / Edit)

Keep the same search + filter + action button layout used in Organizations Directory.

2. Roles Page

Convert the roles list to the same directory layout and rename the section:

“Roles Directory”

Fields inside the table:

Role Name

Description

Assigned Users Count

Created Date

Status

Actions (View / Edit / Manage Permissions)

Maintain identical table styling and spacing from Organizations Directory.

3. Audit Logs Page

Update the activity list to follow the same directory pattern and rename the section:

“Recent Activity Logs”

Fields inside the table:

Activity

User

Module

Date & Time

Status

Actions (View Details)

Design Rules

Use the same card container and table component from Organizations Directory

Keep consistent typography, spacing, row height, and icon style

Maintain the dashboard red theme

Use identical pagination and search interaction

Ensure visual consistency across all pages

The goal is to create a unified directory-style system across the admin dashboard.