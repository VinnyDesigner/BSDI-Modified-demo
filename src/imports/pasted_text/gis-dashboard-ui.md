The map should act as the full background container.

All UI elements must be aligned using an 8px grid system and stay within safe margins from the map edges.

Top Search Panel

Place the search/filter bar centered at the top of the map.

The container should include the following fields in a single horizontal row:

Layer – Dropdown (Select Layer)

Field – Dropdown (Select Field)

Operator – Dropdown (=, Like, Contains)

Value – Input field (Enter Value)

Search Button – Primary button with search icon

The container should:

Have 24px padding

16px gap between inputs

8px rounded corners

soft shadow

semi-transparent white background (90%)

Width should auto-fit content but stay centered on the map

Left Side Panel

Place Drawing Tools panel on the left side of the map.

Align it below the search bar with consistent margin (32px from top).

Panel should include:

Title: Drawing Tools

Button: Draw Polygon

Use:

16px padding

12px spacing

rounded card style

subtle shadow

Right Map Controls

Place map action icons vertically aligned on the right side.

Icons:

Zoom In

Zoom Out

Layers

Draw / Polygon

Each button should:

be 40px circular

have 12px spacing between buttons

be aligned in a vertical floating toolbar

positioned 24px from the right edge and vertically centered

Alignment Rules

Maintain consistent margins from map edges (24px).

All floating UI cards should not overlap each other.

Maintain visual hierarchy:

Search bar → Primary control

Drawing tools → Secondary

Map icons → Utility controls

Ensure the layout works across large screens and 32-inch monitors without excessive empty space.

Style

Modern GIS dashboard UI

Light neutral colors

Soft shadows

Rounded cards

Minimal and clean layout similar to ArcGIS / Mapbox dashboards.